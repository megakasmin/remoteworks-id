export const metadata = {
  title: "Job Detail — RemoteWorks ID",
  description:
    "View job details and apply for remote or hybrid opportunities on RemoteWorks ID.",
};

export default function JobDetailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
